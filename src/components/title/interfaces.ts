export default interface ITitleProps {
    minorTitle: {
        text: string,
        color?: string,
        size?: string
    }
    mainTitle: {
        text: string,
        color?: string,
        size?: string
    }
}